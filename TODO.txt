- add pipelines
    - generate feed separately from HTML
        - pull content (body) from original HTML, not from decorated
    - generate ICML, etc., separately

- save compressed versions of files
    - write compressed versions along with non-compressed
    - serve compressed versions if asked by client