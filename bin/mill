#!/usr/bin/env ruby

require 'mill'

load Path.pwd / 'code' / 'site.rb'

begin
  case ARGV.shift
  when nil, 'make'
    $site.make
  when 'list'
    $site.list
  else
    raise "Unknown command: #{command}"
  end
rescue Mill::Error => e
  warn e
  exit(1)
end